# MultiTerm Astro 博客项目完全指南

> 项目地址：~/my-blog | 线上地址：https://iyangjike.github.io
> 主题：MultiTerm Astro | 默认配色：Dracula | 可选配色：60 种

---

## 一、项目结构总览

```
my-blog/
├── astro.config.mjs          # Astro 配置（插件、集成）
├── tsconfig.json             # TypeScript 配置
├── package.json              # 依赖和脚本
├── src/
│   ├── site.config.ts        # ★ 站点核心配置（标题、作者、导航、配色、社交链接等）
│   ├── types.ts              # TypeScript 类型定义
│   ├── utils.ts              # 工具函数（排序、标签分组、系列分组等）
│   ├── env.d.ts              # 环境变量类型声明
│   ├── content.config.ts     # 内容集合 Schema 定义（定义文章/首页/附言的字段结构）
│   ├── styles/
│   │   └── global.css        # 全局样式
│   ├── content/              # ★ 内容目录 — 所有文章和页面内容放这里
│   │   ├── home.md           # 首页内容（头像、GitHub Calendar、欢迎文字）
│   │   ├── addendum.md       # 文章底部附言（每篇文章末尾都会显示）
│   │   ├── avatar.jpg        # 社交卡片默认头像
│   │   ├── profile.webp      # 首页头像
│   │   └── posts/            # ★ 博客文章目录
│   │       ├── my-first-post.md      # 单文件文章
│   │       └── post-with-images/     # 带图片的文章（用子目录）
│   │           ├── index.md          # 文章内容
│   │           ├── cover.jpg         # 封面图
│   │           └── screenshot.png    # 文中图片
│   ├── pages/                # 路由页面（文件即路由）
│   │   ├── index.astro       # 首页
│   │   ├── about.md          # /about 页面
│   │   ├── 404.astro         # 404 页面
│   │   ├── posts/
│   │   │   ├── [slug].astro  # /posts/文章名 文章详情页
│   │   │   └── [...page].astro # /posts/2 文章列表分页
│   │   ├── tags/
│   │   │   └── [tag]/[...page].astro  # /tags/标签名 标签页
│   │   ├── series/
│   │   │   └── [slug].astro  # /series/系列名 系列页
│   │   ├── rss.xml.ts        # RSS 订阅源
│   │   ├── robots.txt.ts     # robots.txt
│   │   └── social-cards/
│   │       └── [slug].png.ts # 自动生成社交分享卡片
│   ├── components/           # Astro 组件
│   ├── layouts/              # 页面布局
│   ├── plugins/              # 自定义 Remark/Rehype 插件
│   └── icons/                # SVG 图标
├── public/                   # 静态资源（直接复制到构建输出，不经过 Astro 处理）
│   ├── favicon.svg           # 网站图标
│   ├── profile.webp          # 头像（供 public 引用）
│   └── *.webp                # 角色图片（猫头鹰、独角兽、鸭子）
├── .github/workflows/
│   └── deploy.yml            # GitHub Actions 自动部署配置
└── dist/                     # 构建输出（不要手动编辑，gitignore 已忽略）
```

---

## 二、核心配置文件：src/site.config.ts

这是整个博客的**控制中心**，修改任何站点级设置都在这里。

### 关键配置项

```typescript
const config: SiteConfig = {
  // 网站 URL（部署后改）
  site: 'https://iyangjike.github.io',

  // 站点标题（浏览器标签页显示）
  title: 'iYangJike',

  // 站点描述（SEO + RSS）
  description: 'Web3 开发笔记 — Solidity, React, 智能合约',

  // 作者名（文章底部、SEO）
  author: '杨继珂',

  // SEO 关键词
  tags: ['Web3', 'Solidity', 'React', 'Blockchain', 'Astro'],

  // 字体（JetBrains Mono 是等宽程序员字体）
  font: 'JetBrains Mono Variable',

  // 每页显示文章数
  pageSize: 6,

  // 导航栏链接 ★ 新增 Tab 在这里
  navLinks: [
    { name: 'Home', url: '/' },
    { name: 'About', url: '/about' },
    { name: 'Archive', url: '/posts' },
    { name: 'GitHub', url: 'https://github.com/iYangJike', external: true },
  ],

  // 配色方案
  themes: {
    mode: 'select',           // single | select | light-dark-auto
    default: 'dracula',       // 默认主题
    include: [                // 可选主题列表（60 种）
      'dracula', 'nord', 'tokyo-night', 'catppuccin-mocha', ...
    ],
  },

  // 社交链接（页脚显示）
  socialLinks: {
    github: 'https://github.com/iYangJike',
    twitter: 'https://x.com/iYangJike',
    rss: true,
  },

  // Giscus 评论（已禁用，需要时配置 GitHub Discussions）
  giscus: undefined,

  // 角色（文章中的 :::owl 等对话卡片，图片放在 public/ 下）
  characters: {
    owl: '/owl.webp',
    unicorn: '/unicorn.webp',
    duck: '/duck.webp',
  },
}
```

---

## 三、如何写文章

### 文章存放位置

所有文章放在 `src/content/posts/` 目录下。

### 纯文字文章

直接在 `src/content/posts/` 下创建 `.md` 文件：

```markdown
---
title: '我的第一篇文章'
published: 2026-08-21
draft: false
description: '这是一篇关于 Solidity 开发的笔记'
tags: ['Solidity', 'Web3', '智能合约']
toc: true
series: 'Solidity 入门'    # 可选，属于哪个系列
---

## 第一节

文章正文写在这里，支持完整的 Markdown 语法。

## 代码块

​```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

contract HelloWorld {
    string public greeting = "Hello Web3!";
}
​```
```

### 带图片的文章

创建子目录，图片和文章放一起：

```
src/content/posts/my-article/
├── index.md       # 文章内容
├── cover.jpg      # 封面图
└── diagram.png    # 文中图片
```

Frontmatter 中引用封面：

```markdown
---
title: '带图片的文章'
published: 2026-08-21
draft: false
coverImage:
  src: './cover.jpg'
  alt: '封面图描述'
tags: ['React']
toc: true
---

文中引用图片：

![示意图](./diagram.png)
```

### Frontmatter 字段说明

| 字段 | 必填 | 类型 | 说明 |
|------|------|------|------|
| `title` | ✅ | string | 文章标题 |
| `published` | ✅ | Date | 发布日期（决定排序） |
| `draft` | ❌ | boolean | 草稿，`true` 时生产环境不显示 |
| `description` | ❌ | string | 摘要。不填则自动取正文第一段 |
| `tags` | ❌ | string[] | 标签，如 `['Solidity', 'React']` |
| `series` | ❌ | string | 所属系列名，同系列文章会聚合 |
| `toc` | ❌ | boolean | 是否显示目录，默认 `true` |
| `coverImage` | ❌ | object | 封面图，`{ src: './cover.jpg', alt: '描述' }` |
| `author` | ❌ | string | 覆盖默认作者 |

### 文章特殊功能

#### 提示框（Admonitions）

```markdown
:::tip
这是一个提示
:::

:::note
这是一个笔记
:::

:::important
重要信息
:::

:::warning
警告信息
:::

:::caution
注意
:::
```

#### 角色对话卡片

```markdown
:::owl
猫头鹰说的话
:::

:::duck
鸭子说的话
:::

:::unicorn
独角兽说的话
:::
```

角色图片在 `public/` 目录，配置在 `site.config.ts` 的 `characters` 字段。

#### GitHub 卡片

```markdown
:::github[ethereum/solidity]
:::
```

#### 数学公式（KaTeX）

```markdown
行内公式 $E = mc^2$

块级公式：
$$
\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$
```

#### Emoji 短码

```markdown
:solidity: :rocket: :fire:
```

---

## 四、如何新增 Tab（导航栏链接）

编辑 `src/site.config.ts` 的 `navLinks` 数组：

```typescript
navLinks: [
  { name: 'Home', url: '/' },
  { name: 'About', url: '/about' },
  { name: 'Archive', url: '/posts' },
  // 新增内部页面 Tab
  { name: 'Projects', url: '/projects' },
  // 新增外部链接 Tab
  { name: 'GitHub', url: 'https://github.com/iYangJike', external: true },
  { name: 'Twitter', url: 'https://x.com/iYangJike', external: true },
],
```

### 如果是内部页面

在 `src/pages/` 下创建对应的 `.md` 或 `.astro` 文件：

```
src/pages/projects.md   → 访问 /projects
src/pages/tools.astro   → 访问 /tools
```

`.md` 文件示例：

```markdown
---
title: 我的项目
description: 我参与的开源项目
---

## Web3 项目

- [Project A](https://github.com/...)
- [Project B](https://github.com/...)
```

---

## 五、图片放在哪里

| 用途 | 存放位置 | 引用方式 |
|------|---------|---------|
| 文章封面图 | `src/content/posts/文章目录/cover.jpg` | frontmatter: `coverImage.src: './cover.jpg'` |
| 文章内图片 | `src/content/posts/文章目录/pic.png` | Markdown: `![alt](./pic.png)` |
| 首页头像 | `src/content/profile.webp` | `home.md` frontmatter: `avatarImage.src: './profile.webp'` |
| 社交卡片头像 | `src/content/avatar.jpg` | `site.config.ts`: `socialCardAvatarImage` |
| 网站图标 | `public/favicon.svg` | 自动 |
| 角色图片 | `public/*.webp` | `site.config.ts` characters |
| 静态资源（PDF 等） | `public/` | 直接 `/filename.pdf` |

### 重要规则

- **`public/` 下的文件**：直接复制到构建输出，不经过 Astro 处理，路径不变
- **`src/content/posts/文章目录/` 下的图片**：经过 Astro 图片优化（压缩、转格式、生成多尺寸）
- **封面图用相对路径**：`./cover.jpg` 而不是 `/cover.jpg`

---

## 六、首页配置

编辑 `src/content/home.md`：

```markdown
---
avatarImage:
  src: './profile.webp'
  alt: '我的头像'
githubCalendar: 'iYangJike'   # GitHub 用户名，显示贡献日历
---

欢迎来到我的博客！这里记录我的 Web3 开发笔记。
```

- `githubCalendar` 设为你的 GitHub 用户名，会在首页显示 GitHub 贡献热力图
- `avatarImage.src` 指向头像图片（相对路径）

---

## 七、文章底部附言

编辑 `src/content/addendum.md`：

```markdown
---
avatarImage:
  src: './profile.webp'
  alt: '我的头像'
---

感谢阅读！欢迎在 [GitHub](https://github.com/iYangJike) 上找到我。
```

这个内容会显示在**每篇文章的末尾**。

---

## 八、部署流程

### 自动部署（已配置）

```bash
# 1. 写文章
vim src/content/posts/my-post.md

# 2. 本地预览（可选）
npm run dev          # http://localhost:4321

# 3. 提交推送
git add -A
git commit -m "新文章：xxx"
git push
```

推送后 GitHub Actions 自动构建部署，约 1 分钟后生效。

### 本地预览

```bash
cd ~/my-blog
npm run dev
```

### 本地构建

```bash
cd ~/my-blog
npm run build       # 输出到 dist/
npm run preview     # 预览构建结果
```

---

## 九、常用命令

```bash
npm run dev         # 启动开发服务器
npm run build       # 生产构建
npm run preview     # 预览构建结果
npm run astro ...   # 运行 Astro CLI 命令
```

---

## 十、高级定制

### 修改配色

在 `src/site.config.ts` 中修改 `themes.default`，可选值包括：

dracula, nord, tokyo-night, catppuccin-mocha, catppuccin-latte, gruvbox-dark-hard, gruvbox-light-soft, monokai, one-dark-pro, rose-pine, rose-pine-moon, solarized-dark, synthwave-84, vesper, github-dark, github-light...

共 60 种，完整列表见 `site.config.ts` 的 `themes.include`。

### 自定义主题颜色

在 `site.config.ts` 的 `themes.overrides` 中覆盖特定主题的颜色：

```typescript
overrides: {
  'dracula': {
    accent: '#ff79c6',
    link: '#8be9fd',
  },
}
```

可覆盖的颜色键：`foreground`, `background`, `accent`, `heading1-6`, `link`, `separator`, `note`, `tip`, `important`, `caution`, `warning` 等。

### 添加 Giscus 评论

1. 去 https://giscus.app 配置
2. 在 `site.config.ts` 中填入：

```typescript
giscus: {
  repo: 'iYangJike/iYangJike.github.io',
  repoId: 'R_kgXXX',
  category: 'Comments',
  categoryId: 'DIC_kwXXX',
  reactionsEnabled: true,
},
```

### 自定义 CSS

编辑 `src/styles/global.css`。

---

## 十一、常见问题

**Q: 文章不显示？**
检查 `draft: true`，生产环境草稿不会显示。

**Q: 图片不显示？**
检查路径是否为相对路径（`./image.png` 而不是 `/image.png`），且图片在文章目录下。

**Q: 修改配置后没生效？**
配置修改后需要重新构建部署，本地用 `npm run dev` 会自动热更新。

**Q: 如何更换 favicon？**
替换 `public/favicon.svg`。
